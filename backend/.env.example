# ============================================
# ENVIRONMENT VARIABLES TEMPLATE (.env.example)
# Copy this to .env and fill in your values
# ============================================

# Database Configuration
# Get your MongoDB Atlas connection string from:
# MongoDB Atlas Dashboard → Connect → Connect your application
# Format: mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/DATABASE?retryWrites=true&w=majority
# If empty, server uses local MongoDB (mongodb://127.0.0.1:27017/portfolio)
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.mongodb.net/portfolio?retryWrites=true&w=majority

# Server Configuration
NODE_ENV=development
PORT=5000
CORS_ORIGIN=http://localhost:3000

# Email Configuration (Gmail)
# Get your app password from: https://myaccount.google.com/security
# App passwords section → Select Mail → Generate password (16 characters)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=16-character-app-password
EMAIL_RECEIVER=your-email@gmail.com
EMAIL_SERVICE=gmail
EMAIL_FROM_NAME=Portfolio Contact Form
